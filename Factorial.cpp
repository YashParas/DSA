#include<bits/stdc++.h>
using namespace std;
int factorial(int n);
{
	if(n<0)
	return -1;
	if(n==0)
	return 1;
	else
	{
		return(n*factorial(n-1));
	}
	
}
int main()
{
	int factorial(int);
	int fact, num;
	cout<<"Enter any number";
	cin>>num;
	n=factorial(n);
	cout<<"Factorial of"<<" "<<n<<" is : "<<fact<<endl;
}